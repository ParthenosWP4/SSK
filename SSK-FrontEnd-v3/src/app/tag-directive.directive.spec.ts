import { TagDirectiveDirective } from './tag-directive.directive';

describe('TagDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TagDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
