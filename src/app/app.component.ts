import { Component } from '@angular/core';
import '../main.js';

let styles = String(require('../styles.scss'));
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
 
  
})

export class AppComponent {
  title = 'app works!';
}
