import { ChangeOnDivDirective } from './change-on-div.directive';

describe('ChangeOnDivDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeOnDivDirective();
    expect(directive).toBeTruthy();
  });
});
